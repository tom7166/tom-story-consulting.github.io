name: Move Website Fileson:  workflow_dispatch:jobs:  move-files:    runs-on: ubuntu:jobs:  move-files:    runs-on: ubuntu-latest        steps:      - uses: actions/checkout@v2            - name: Move files to root directory        run: |          ls -la          cp -r "Storytom-Website Root/public/"* ./          rm -rf "Storytom-Website Root"          ls -la                - name: Commit changes        run: |          git config --local user.email "action@github.com"          git config --local user.name "GitHub Action"          git add -A          git commit -m "Move website files to root directory"          git push-latest        steps:      - uses: actions/checkout@v2            - name: Move files to root directory        run: |          cp -r "Storytom-Website Root/public/"* ./          rm -rf "Storytom-Website Root"                - name: Commit changes        run: |          git config --local user.email "action@github.com"          git config --local user.name "GitHub Action"          git add -A          git commit -m "Move website files to root directory"          git push
name: Move Website Fileson:  workflow_dispatch
